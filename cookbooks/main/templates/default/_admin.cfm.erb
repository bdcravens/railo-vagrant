<cftry>
<cfadmin 
	action="updatePassword"
	type="server"
	newPassword="<%= node[:railo][:server_password] %>">
<cfcatch></cfcatch></cftry>

<cftry>
<cfadmin 
	action="updatePassword"
	type="web"
	newPassword="<%= node[:railo][:web_password] %>">
<cfcatch></cfcatch></cftry>

<!--- 
below is where you'd do other config steps you can only do from Railo - 
mostly this will be done via cfadmin tags
--->
